# Development-only flags. Copy to `.env.local` (which is gitignored) and edit.

# When 'true', the app loads pre-saved GBFS snapshots from src/fixtures/ instead
# of polling the live feed. The rolling window fills instantly so visual changes
# can be iterated against frozen data. Never set this in a production build.
VITE_USE_FIXTURES=true
